import React from 'react'
import Typography from '@material-ui/core/Typography';

export default function header() {
    return (
        <div style={{padding: 50}}>
            <Typography style={{color: 'silver'}} variant="h2">Valiutu kursai pagal LB</Typography>
        </div>
    )
}
